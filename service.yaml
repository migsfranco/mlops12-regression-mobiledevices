apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: servicio-streamlit-datapath-miguel-franco
spec:
  template:
    spec:
      containers:
        - image: us-central1-docker.pkg.dev/project-mlops12-miguel-franco/repo-mlops12-streamlit-regresion/image-v2-streamlit-datapath:latest
          env:
          ports:
            - containerPort: 4000